@page "/fetchdata"
@using Panel.Data
@inject SkabenAlertDaemon alertService

@* <PageTitle>Weather forecast</PageTitle>

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from a service.</p>

@if (forecasts == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Date</th>
                <th>1</th>
                <th>Много текста</th>
                <th>@forecasts.Length</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var forecast in forecasts)
            {
                <tr>
                    <td>@forecast.Date.ToShortDateString()</td>
                    <td>@forecast.IsNotLocked</td>
                    <td>@forecast.Text</td>
                    <td>@forecast.Locked</td>
                </tr>
            }
        </tbody>
    </table> *@
}

@code {
    private TerminalState[]? forecasts;


}
